{
  "name": "dvpn-mvp",
  "version": "1.0.0",
  "description": "Decentralized VPN MVP with smart contracts and off-chain components",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "node": "hardhat node",
    "start-vpn": "cd vpn_node && npm run build && npm start",
    "start-vpn-dev": "cd vpn_node && npm run dev",
    "start-app": "cd mobile_app && npm start",
    "start-api": "node api/server.js",
    "setup": "npm install && cd vpn_node && npm install && npm run build"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.7.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "wireguard-tools": "^0.4.0",
    "node-cron": "^3.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "chai": "^4.3.7",
    "ethereum-waffle": "^4.0.10"
  },
  "keywords": ["vpn", "blockchain", "decentralized", "wireguard"],
  "author": "dVPN Team",
  "license": "MIT"
} 